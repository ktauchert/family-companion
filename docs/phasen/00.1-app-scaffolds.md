# Phase 0.1 — App-Scaffolds

- Status: **PR offen**
- Branch: `phase-0.1-app-scaffolds`
- Ergebnis: Lauffähige leere Next.js- und Expo-Apps im Monorepo, beide importieren `@family-companion/shared`

## Arbeitspakete

### WP1 — Next.js Web

- [x] Next.js (App Router) in `apps/web`
- [x] `@family-companion/shared` importierbar
- [x] Placeholder-Household entfernen, sobald eine echte Seite steht

### WP2 — Expo Mobile

- [x] Expo + Expo Router in `apps/mobile`
- [x] `@family-companion/shared` importierbar (Metro/Workspace)
- [x] Placeholder entfernen, sobald ein Screen steht

### WP3 — Turbo

- [x] `dev` / `build` / `lint` in beiden Apps echt (kein Echo-Stub)
- [x] `npm run dev` startet Web und/oder Mobile über Turbo

### WP4 — Monorepo-React *(ergänzt)*

- [x] Eine React-Version (19.2.3) per Root-`overrides`, damit Next und Expo nicht zwei Reacts hoisten

## Definition of Done

Die Phase ist fertig, wenn beide Apps lokal starten, Shared importieren, die Placeholder weg sind, Turbo echte Scripts hat und ein PR gegen `main` liegt. Kein Produkt-Feature, kein Login, keine CI-Pflicht.

## Prüfung

Lokal (kein CI in dieser Phase, [ADR 0002](../adr/0002-unit-tests-und-github-ci.md)):

- `npm run type-check`
- `npm run lint`
- `npm run build`
- Web: Startseite zeigt Shared-Konstante (`FREE_TIER_MAX_MEMBERS`)
- Mobile: Expo startet / `expo export` bündelt Shared

Review am PR: Scaffolds in `apps/web` und `apps/mobile`, Shared nur über `@family-companion/shared`, Scope hält, Docs-Haken stimmen. Maintainer mergt.
